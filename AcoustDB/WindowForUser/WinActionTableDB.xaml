<Window x:Class="AcoustDB.WindowForUser.WinActionTableDB"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AcoustDB.WindowForUser"
        mc:Ignorable="d"
        
        xmlns:gl="clr-namespace:System.Globalization;assembly=mscorlib"
        xmlns:viewmodels="clr-namespace:ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:ProgrammGlobalFuncViewModel}"

        WindowStartupLocation="CenterScreen"
        Title="База данных" 
        Height="500" Width="1100" MinHeight="500" MinWidth="850">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

            </ResourceDictionary.MergedDictionaries>


        </ResourceDictionary>
    </Window.Resources>


    <Border Style="{StaticResource WindowChromeStyleHead}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="0*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="0"/>
            </Grid.RowDefinitions>

            
            
            
            
            
            
            
        </Grid>
    </Border>
</Window>
